Build the Quadrado project and copy the contents of the Deploy_Quadrado directory next to this file (into this project's root folder).

Also be sure to set your ANDROID_HOME environment variable is set to your Android SDK's install folder, which should be similar to "android-sdk".
You probably have $ANDROID_HOME/tools and $ANDROID_HOME/platform-tools on your PATH variable if you've installed the Android SDK.